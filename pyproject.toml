[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "create-eas-arcgis"
version = "0.1.0"
description = "Candidate census Enumeration Area generation for ArcGIS Pro"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
